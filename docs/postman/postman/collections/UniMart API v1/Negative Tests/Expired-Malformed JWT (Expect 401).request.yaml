$kind: http-request
name: Expired/Malformed JWT (Expect 401)
url: "{{base_url}}/listings"
method: POST
body:
  type: json
  content: |-
    {
      "title": "Unreachable Book",
      "description": "Fails token checks.",
      "price": 10.00,
      "categoryId": 1
    }
auth:
  type: bearer
  credentials:
    token: "{{auth_secret_0u7s}}"
order: 8000
