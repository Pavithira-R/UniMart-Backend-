$kind: http-request
url: "{{base_url}}/reviews"
method: POST
body:
  type: json
  content: |-
    {
      "orderId": 2,
      "rating": 5,
      "comment": "Review order that is pending/cancelled"
    }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 5000
